# Release Notes

## Unreleased

RapidMX's desktop client. An Electron shell built from the same `@rapidmx/web-client` and
`@rapidmx/react-shared` source that also builds the web client and `rapidmx/server`'s own
server-rendered pages.
